package Notlar;

public class T21_Notlar {
    // Static olarak tanımlanan bir metoda program çalıştığı sürece erişilir,
    // böylece sadece bir metot ile birden çok nesne çağırılır.6
    // obje o metotat gider.
    // obje ile yapılır.
    //Class variableda yer alanlar instance variable dır
    // void MyClass(){
    // //num1++;// paramereretseiz ama return type ı olduğu için constrroctor değildir.
    // // metot olması için void ya da return type olması gerekir
    // this keyword instance variablelar ile ilişkilidr.
    // default varsayılan demektir
    // obje ister static olsun ister statik olmasın
    // obje oluturarak oluşan veriler sadece o objeye has kalır
    // class variable da oluşturduğumuz ister static
    // istersek de non static  buna da instance variable deriz
    // static variable ın dier adı class variable
    //instance variable obje variable
    // static variable lar tüm class dan kullanılabilirken
    // main metot statik kulubüne üye statik olmayanla işi olmaz
    //sout(sayi) day 27 de hata verdi
    /*
    instance olanlar static olmayan methodlar kullanılabir

         instance variable lara static method dan ulaşmak istersek
         obje oluşturmamız gerekir.
         obje oluşturmak için clas adı*/
    // main metot da instanca variable a obje üzerinden kulırız
    //obj1.rakam   ama sayi+=1; deriz çünkü bu statik
    /*statik bir variablea ulaşmak için obje oluşturmaya gerek yoktur
    ona ulaşmak için aynı clas içerisinde ise direl ismi farklı clas içerisinde ise
    Clas adı. istediğimiz metot ya da variable
    instance için mutlaka objeye ihtiyaç vardır
    static kelimesi bir vaiable ı metotu clasa bağlamak için kullanılır
    static variable clas yüklendiğinde hemen memory static variablelar oluşturur.
    static variablelar bir tane oluşturulur ve clas daki tüm objeler onu görür ve kullanır
    okul ve bir öğrenci örneği
    static int count = 0;

       count da ayrıcalıklı olan  mtott  değil variable heryere gider
       yani count her yerden kullanılabilir
      objnin altında sout o objeyi bağlar obje nin altındaki obj 1 soutu
      obj 2 ye göre çalışır
      bir metodu statik yapmak istiyorsak return type ından önce statik yazmamız yeterli
      public static void add(){
      get istediğimiz indexdeki elemanı döndürmek için
      split metodu parçalara ayırır ve arryin için ekoyar
      Split o boşlukları yok eder onları geriye kalanları birer eleman gibi yazar
      Belirtili şeyi yok ediyor öncesini ve sonrasını elelman olarak yazar
      "" ile split tüm hiçlikleri ayırr
      nesne ile constructora gideriz

      Passbyvalue biz javada program yaparken genelde constructorlardada aynısını yapmıştık
      main metotda kullandığımız variable ismi ne ise kodu rahat takip edebilmek için construc
      tor da da variable dada aynı ism kullanmayı tercih ederiz
      ama biz programı çalıştıtırkne sürekli scope lar arası hareket ederiz
      aynı scope da aynı isimle iki tane variable oluşturamayız
      }







     */


}
